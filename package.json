{
  "name": "offsystem-monorepo",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20.19" },
  "packageManager": "pnpm@11.23.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
