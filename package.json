{
  "name": "dscheck-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "packageManager": "pnpm@11.23.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome check --write .",
    "typecheck": "pnpm -r --filter \"./packages/*\" exec tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@changesets/cli": "^3.0.1",
    "@vitest/coverage-v8": "^3.2.7",
    "fast-check": "^4.9.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
