# Absence drill — 2026-08-24

Protocol (RUNBOOK N5): fresh clone, no local state, execute the runbook strictly
by-the-book. Performed against `oddurs/dscheck@main`.

| step | runbook section | result |
|---|---|---|
| `git clone` → `pnpm install --frozen-lockfile` → build → typecheck → test | Release (prereqs) | ✔ 118/118 tests, zero undocumented steps |
| release dry-run: changeset add → `changeset status` → `changeset version` | Release | ✔ correct dependency cascade (core patch → all dependents), versions applied in clone |
| corpus from committed bundles (`DSCHECK_CORPUS_OFFLINE=1`) | CI health / corpus re-pin | ✔ 5/5 repos exact counts, exit 3 signalled availability |
| FP fixture protocol: new `fixtures/fp/005-*` case → fp suite | FP triage | ✔ suite auto-discovered the case, 6/6 |

Gaps found: none — every step was already documented. Next drill: after the first real
release (add: verify provenance badge + scoreboard row steps).
